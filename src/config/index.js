const config = {
  port: process.env.PORT || 3000,
  appName: 'Jnanesh Sharma H. — Portfolio API',
  serverStartTime: Date.now(),
  timezone: 'Asia/Kolkata',
  location: 'India'
};

module.exports = config;
